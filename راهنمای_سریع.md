# راهنمای سریع - اصلاحات سیستم

## ✅ مشکلات برطرف شده

### 1. توکن HuggingFace منقضی شده
**راه‌حل:** توکن جدید در فایل `.env` قرار داده شد
- همه سرویس‌ها حالا به توکن جدید دسترسی دارند
- مدل‌های HuggingFace قابل استفاده هستند

### 2. خطای HTTP 451 از Binance
**راه‌حل:** سیستم Fallback خودکار
- اگر Binance کار نکند → از CoinGecko استفاده می‌شود
- اگر CoinGecko کار نکند → از HuggingFace استفاده می‌شود
- همیشه داده در دسترس است

### 3. مشکلات دریافت اخبار
**راه‌حل:** RSS feeds به‌روزرسانی شد
- منابع خبری جدید اضافه شد (Decrypt, Bitcoinist, CryptoSlate)
- مدیریت خطای بهتر
- تلاش مجدد خودکار

### 4. مشکلات OHLCV (داده‌های بازار)
**راه‌حل:** سیستم 4 مرحله‌ای
1. Binance (سریع‌ترین)
2. CoinGecko (بدون محدودیت)
3. HuggingFace (پشتیبان)
4. Demo (همیشه کار می‌کند)

---

## 📋 فایل‌های تغییر یافته

1. `.env` - توکن‌های جدید
2. `backend/services/binance_client.py` - مدیریت HTTP 451
3. `backend/services/coingecko_client.py` - متد OHLCV جدید
4. `backend/services/crypto_news_client.py` - RSS feeds بهبود یافته
5. `backend/services/ohlcv_service.py` - سیستم fallback 4 لایه

---

## 🚀 نحوه استفاده

### اجرای سیستم:
```bash
cd /workspace
python3 main.py
```

### تست سیستم:
```bash
python3 test_fixes.py
```

### مشاهده logs:
```bash
tail -f crypto_data.log
```

---

## ⚙️ تنظیمات پیشنهادی

### برای دسترسی بهتر به Binance:
1. استفاده از VPN
2. یا اجازه دهید سیستم از CoinGecko استفاده کند (خودکار)

### برای اخبار بیشتر (اختیاری):
در فایل `.env` اضافه کنید:
```env
NEWSAPI_KEY=کلید_شما
CRYPTOPANIC_TOKEN=توکن_شما
```

### برای داده‌های بیشتر (اختیاری):
```env
COINMARKETCAP_API_KEY=کلید_شما
ETHERSCAN_API_KEY=کلید_شما
BSCSCAN_API_KEY=کلید_شما
```

---

## 📊 وضعیت فعلی

| سرویس | وضعیت | توضیح |
|---|---|---|
| HuggingFace | ✅ | توکن جدید فعال |
| Binance | ⚠️ | محدودیت منطقه‌ای (fallback فعال) |
| CoinGecko | ✅ | جایگزین اصلی |
| News Feeds | ✅ | 5 منبع فعال |
| Workers | ✅ | آماده اجرا |

---

## 🔍 عیب‌یابی

### اگر داده‌ها دریافت نمی‌شود:
1. بررسی اتصال اینترنت
2. اجرای `python3 test_fixes.py`
3. بررسی logs

### اگر توکن HuggingFace کار نمی‌کند:
1. بررسی فایل `.env` وجود دارد
2. بررسی توکن صحیح در .env قرار دارد
3. راه‌اندازی مجدد سرویس

### اگر Binance HTTP 451 می‌دهد:
- این طبیعی است! سیستم به طور خودکار از CoinGecko استفاده می‌کند
- نیازی به کاری نیست

---

## 📝 یادداشت‌های مهم

1. **امنیت**: فایل `.env` را به repository عمومی push نکنید
2. **Fallback**: سیستم به طور خودکار منابع جایگزین را امتحان می‌کند
3. **Logs**: همیشه logs را برای عیب‌یابی بررسی کنید

---

## 📞 کمک بیشتر

اگر مشکلی باقی ماند:
1. فایل `FIXES_SUMMARY.md` را بخوانید (جزئیات کامل)
2. `test_fixes.py` را اجرا کنید
3. logs را بررسی کنید

---

تاریخ: 6 دسامبر 2025
نسخه: 1.0
