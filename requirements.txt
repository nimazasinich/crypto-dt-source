# Core FastAPI and Server
fastapi==0.115.0
uvicorn[standard]==0.31.0
python-multipart==0.0.9

# HTTP Clients
httpx==0.27.2
aiohttp==3.10.5
requests==2.32.3

# WebSocket
websockets==13.1
python-socketio==5.11.4

# Data Processing
pydantic==2.9.2
python-dotenv==1.0.1
feedparser==6.0.11
pandas==2.3.3

# Database
sqlalchemy==2.0.35
alembic==1.13.3
aiosqlite==0.20.0

# Async Support
aiofiles==24.1.0

# Scheduling
apscheduler==3.10.4

# File watching
watchdog==6.0.0

# DNS resolution
# Keep compatible with Python 3.9 CI
dnspython==2.7.0

# HuggingFace (optional - for AI models)
# Version constraints:
# - transformers 4.35.0 requires huggingface-hub>=0.16.4,<1.0
# - datasets 4.4.1 requires huggingface-hub>=0.25.0,<2.0
# - Compatible range: 0.25.0 <= huggingface-hub < 1.0
huggingface-hub>=0.19.4,<1.0  # Compatible with both transformers and datasets
datasets>=2.14.0  # More flexible version

# Utilities
python-dateutil==2.9.0
pytz==2024.2
psutil==6.1.0

# Security and Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# AI/ML DEPENDENCIES (CPU-ONLY - optimized for HuggingFace Spaces)
# CPU-only torch and transformers to avoid GPU dependencies and reduce build time
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
transformers==4.35.0
# numpy is auto-installed with pandas
