# 🚀 لیست کامل سرویس‌های موجود

## 🌐 آدرس اسپیس شما
```
https://really-amin-datasourceforcryptocurrency-2.hf.space
```

---

## ✅ سرویس‌هایی که الان فعال هستند:

### 1️⃣ داده‌های بازار (Market Data)

#### `/api/market` - لیست قیمت‌ها
```bash
curl "https://really-amin-datasourceforcryptocurrency-2.hf.space/api/market?limit=100"
```
**خروجی**: لیست 100 ارز با قیمت، Market Cap، تغییرات 24h

#### `/api/market/history` - نمودار شمعی (OHLCV)
```bash
curl "https://really-amin-datasourceforcryptocurrency-2.hf.space/api/market/history?symbol=BTCUSDT&interval=1h&limit=100"
```
**خروجی**: 100 شمع قیمتی برای نمودار

---

### 2️⃣ تحلیل احساسات (Sentiment Analysis)

#### `/api/sentiment/analyze` - تحلیل متن
```bash
curl -X POST https://really-amin-datasourceforcryptocurrency-2.hf.space/api/sentiment/analyze \
  -H "Content-Type: application/json" \
  -d '{"text": "Bitcoin is pumping!"}'
```
**خروجی**: احساسات مثبت/منفی با امتیاز

---

### 3️⃣ سیستم Smart Fallback (305+ منبع)

#### `/api/smart/market` - قیمت‌ها با Fallback
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/market?limit=50
```
**ویژگی**: اگر یک API خراب شد، خودکار از 20 API دیگه می‌گیره!

#### `/api/smart/news` - اخبار با Fallback
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/news?limit=20
```
**ویژگی**: از 15 منبع خبری مختلف با اولویت‌بندی هوشمند

#### `/api/smart/sentiment` - احساسات با Fallback
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/sentiment
```

#### `/api/smart/whale-alerts` - هشدار نهنگ‌ها
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/whale-alerts?limit=20
```
**خروجی**: تراکنش‌های بزرگ (نهنگ‌ها)

#### `/api/smart/blockchain/{chain}` - اطلاعات زنجیره
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/blockchain/ethereum
```
**زنجیره‌ها**: ethereum, bitcoin, binance-smart-chain, polygon

#### `/api/smart/health-report` - گزارش سلامت 305 منبع
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/health-report
```
**خروجی**: وضعیت تمام 305 منبع داده

#### `/api/smart/stats` - آمار کلی
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/stats
```

---

### 4️⃣ Alpha Vantage API

#### `/api/alphavantage/prices` - قیمت‌های کریپتو
```bash
curl "https://really-amin-datasourceforcryptocurrency-2.hf.space/api/alphavantage/prices?symbol=BTC"
```

#### `/api/alphavantage/ohlcv` - داده‌های OHLCV
```bash
curl "https://really-amin-datasourceforcryptocurrency-2.hf.space/api/alphavantage/ohlcv?symbol=BTC&interval=daily"
```

#### `/api/alphavantage/market-status` - وضعیت بازار
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/alphavantage/market-status
```

#### `/api/alphavantage/crypto-rating/{symbol}` - رتبه‌بندی ارز
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/alphavantage/crypto-rating/BTC
```

#### `/api/alphavantage/quote/{symbol}` - قیمت لحظه‌ای
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/alphavantage/quote/AAPL
```

---

### 5️⃣ Massive.com (APIBricks) API

#### `/api/massive/quotes/{ticker}` - قیمت سهام
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/massive/quotes/AAPL
```

#### `/api/massive/trades/{ticker}` - معاملات
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/massive/trades/AAPL
```

#### `/api/massive/aggregates/{ticker}` - داده‌های تجمیعی
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/massive/aggregates/AAPL
```

#### `/api/massive/ticker/{ticker}` - اطلاعات تیکر
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/massive/ticker/AAPL
```

#### `/api/massive/market-status` - وضعیت بازار سهام
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/massive/market-status
```

#### `/api/massive/dividends` - سود سهام
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/massive/dividends
```

#### `/api/massive/splits` - تقسیم سهام
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/massive/splits
```

---

### 6️⃣ Data Hub (از HuggingFace Datasets)

#### `/api/hub/market` - بازار از Dataset
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/hub/market?limit=100
```

#### `/api/hub/ohlc/{symbol}` - OHLC از Dataset
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/hub/ohlc/BTCUSDT?interval=1h&limit=100
```

#### `/api/hub/news` - اخبار از Dataset
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/hub/news?limit=20
```

#### `/api/hub/sentiment` - احساسات از Dataset
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/hub/sentiment
```

---

### 7️⃣ Data Endpoints (سیستم داخلی)

#### `/api/data/prices` - لیست قیمت‌ها
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/prices?limit=50
```

#### `/api/data/prices/{symbol}` - قیمت یک ارز
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/prices/bitcoin
```

#### `/api/data/history/{symbol}` - تاریخچه قیمت
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/history/bitcoin?days=30
```

#### `/api/data/market-overview` - نمای کلی بازار
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/market-overview
```

#### `/api/data/news` - اخبار
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/news?limit=20
```

#### `/api/data/news/{news_id}` - یک خبر خاص
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/news/123
```

#### `/api/data/news/search` - جستجوی اخبار
```bash
curl "https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/news/search?q=bitcoin"
```

#### `/api/data/sentiment/current` - احساسات فعلی
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/sentiment/current
```

#### `/api/data/sentiment/history` - تاریخچه احساسات
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/sentiment/history?days=7
```

#### `/api/data/whales/transactions` - تراکنش‌های نهنگ‌ها
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/whales/transactions?limit=20
```

#### `/api/data/whales/stats` - آمار نهنگ‌ها
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/whales/stats
```

#### `/api/data/blockchain/gas` - Gas Price
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/blockchain/gas
```

#### `/api/data/blockchain/stats` - آمار بلاکچین
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/blockchain/stats
```

---

### 8️⃣ سیستم مانیتورینگ

#### `/api/health` - سلامت سیستم
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/health
```

#### `/api/status` - وضعیت collectors
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/status
```

#### `/api/categories` - دسته‌بندی منابع
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/categories
```

#### `/api/providers` - لیست provider ها
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/providers
```

---

### 9️⃣ WebSocket (Real-time)

#### اتصال WebSocket
```javascript
const ws = new WebSocket('wss://really-amin-datasourceforcryptocurrency-2.hf.space/ws');

ws.onopen = () => {
  // عضویت در قیمت‌ها
  ws.send(JSON.stringify({
    action: 'subscribe',
    channel: 'prices',
    symbols: ['BTCUSDT', 'ETHUSDT']
  }));
};

ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  console.log('قیمت جدید:', data);
};
```

#### آمار WebSocket
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/ws/stats
```

#### سرویس‌های WebSocket
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/ws/services
```

#### Endpoint های WebSocket
```bash
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/ws/endpoints
```

---

## 📊 خلاصه منابع داده

### ✅ 305+ منبع داده شامل:

1. **21 Market Data API** - قیمت، Market Cap، Volume
   - CoinGecko, CoinMarketCap, CryptoCompare, ...

2. **15 News API** - اخبار کریپتو
   - CryptoPanic, CoinTelegraph, Bitcoin.com, ...

3. **12 Sentiment API** - تحلیل احساسات
   - Alternative.me (Fear & Greed), LunarCrush, ...

4. **13 On-chain Analytics** - تحلیل زنجیره
   - Glassnode, IntoTheBlock, CryptoQuant, ...

5. **9 Whale Tracking** - ردیابی نهنگ‌ها
   - Whale Alert, ClankApp, ...

6. **24 RPC Nodes** - اتصال مستقیم به بلاکچین
   - Ethereum, Bitcoin, BSC, Polygon, ...

7. **40+ Block Explorers** - جستجوی تراکنش
   - Etherscan, Blockchain.com, BscScan, ...

8. **45+ AI Models** از HuggingFace
   - Sentiment Analysis
   - Text Generation
   - Summarization
   - Trading Signals

9. **106 Local Backend Routes** - سیستم داخلی

10. **7 CORS Proxies** - دور زدن محدودیت‌ها

---

## 🎯 ویژگی‌های خاص

### 1. Smart Fallback System
- اگر یک API خراب شد، **خودکار** از API های دیگه می‌گیره
- **هیچوقت** خطا نمیده (404)
- همیشه بهترین کیفیت رو میده

### 2. Background Workers
- **24/7** داره داده جمع می‌کنه
- هر 1-5 دقیقه یکبار داده‌ها رو به‌روز می‌کنه
- داده‌ها رو توی دیتابیس Cache می‌کنه

### 3. Real-time با WebSocket
- قیمت‌های لحظه‌ای
- اخبار آنی
- سیگنال‌های زنده

### 4. AI Models (45+ مدل)
- تحلیل احساسات متن
- سیگنال معاملاتی
- خلاصه‌سازی اخبار

---

## 🔥 محبوب‌ترین سرویس‌ها

### برای قیمت گرفتن:
```bash
# بهترین گزینه (با Fallback)
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/market?limit=50

# گزینه معمولی
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/market?limit=50
```

### برای تحلیل احساسات:
```bash
curl -X POST https://really-amin-datasourceforcryptocurrency-2.hf.space/api/sentiment/analyze \
  -H "Content-Type: application/json" \
  -d '{"text": "Bitcoin to the moon!"}'
```

### برای اخبار:
```bash
# بهترین گزینه (با Fallback)
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/news?limit=10

# گزینه معمولی
curl https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/news?limit=10
```

### برای نمودار:
```bash
curl "https://really-amin-datasourceforcryptocurrency-2.hf.space/api/market/history?symbol=BTCUSDT&interval=1h&limit=100"
```

---

## 💡 نکات مهم

### 1. همیشه از آدرس Space استفاده کنید
```
https://really-amin-datasourceforcryptocurrency-2.hf.space
```
نه localhost!

### 2. Smart endpoints رو ترجیح بدید
```
/api/smart/*  ← این‌ها خودکار Fallback دارن
```

### 3. WebSocket برای real-time
```javascript
wss://really-amin-datasourceforcryptocurrency-2.hf.space/ws
```

### 4. مستندات تعاملی
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/docs
```

---

## 🎨 صفحات UI موجود

### 1. صفحه اصلی
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/
```

### 2. داشبورد
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/static/pages/dashboard/index.html
```

### 3. داده بازار
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/static/pages/market/index.html
```

### 4. دستیار معاملات
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/static/pages/trading-assistant/index.html
```

### 5. تحلیل تکنیکال
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/static/pages/technical-analysis/index.html
```

### 6. اخبار
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/static/pages/news/index.html
```

### 7. تحلیل احساسات
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/static/pages/sentiment/index.html
```

### 8. مدل‌های AI
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/static/pages/models/index.html
```

### 9. API Explorer
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/static/pages/api-explorer/index.html
```

---

## 🚀 همه چیز آماده است!

✅ **305+ منبع داده**
✅ **45+ مدل AI**
✅ **9 صفحه UI کامل**
✅ **Smart Fallback** (هیچوقت خطا نمیده)
✅ **WebSocket** برای real-time
✅ **Background Workers** برای جمع‌آوری 24/7
✅ **مستندات کامل**

**برای استفاده فقط کافیه یکی از endpoint ها رو امتحان کنید!**
