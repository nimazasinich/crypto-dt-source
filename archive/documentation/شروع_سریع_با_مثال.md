# 🚀 شروع سریع - مثال‌های عملی

## آدرس اسپیس شما
```
https://really-amin-datasourceforcryptocurrency-2.hf.space
```

---

## 🎯 چطور استفاده کنم؟

### روش 1: مرورگر (ساده‌ترین)

فقط لینک‌ها رو توی مرورگر باز کنید:

#### قیمت 50 ارز برتر
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/market?limit=50
```

#### آخرین اخبار
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/news?limit=10
```

#### سلامت سیستم
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/api/health
```

---

### روش 2: JavaScript (برای وب سایت)

#### مثال 1: گرفتن قیمت‌ها و نمایش
```html
<!DOCTYPE html>
<html>
<head>
  <title>قیمت ارزها</title>
  <style>
    body { font-family: Arial; padding: 20px; }
    .coin { padding: 10px; border: 1px solid #ddd; margin: 5px; }
  </style>
</head>
<body>
  <h1>📊 قیمت ارزها</h1>
  <div id="prices"></div>

  <script>
    async function loadPrices() {
      const url = 'https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/market?limit=10';
      
      try {
        const response = await fetch(url);
        const data = await response.json();
        
        const container = document.getElementById('prices');
        
        data.items.forEach(coin => {
          const div = document.createElement('div');
          div.className = 'coin';
          div.innerHTML = `
            <strong>${coin.name} (${coin.symbol})</strong><br>
            قیمت: $${coin.current_price}<br>
            تغییر 24h: ${coin.price_change_percentage_24h}%
          `;
          container.appendChild(div);
        });
        
      } catch (error) {
        console.error('خطا:', error);
      }
    }
    
    // بارگذاری اولیه
    loadPrices();
    
    // به‌روزرسانی هر 30 ثانیه
    setInterval(loadPrices, 30000);
  </script>
</body>
</html>
```

---

#### مثال 2: تحلیل احساسات
```html
<!DOCTYPE html>
<html>
<head>
  <title>تحلیل احساسات</title>
  <style>
    body { font-family: Arial; padding: 20px; }
    textarea { width: 100%; height: 100px; }
    button { padding: 10px 20px; background: #4CAF50; color: white; border: none; cursor: pointer; }
    #result { margin-top: 20px; padding: 20px; border: 1px solid #ddd; }
  </style>
</head>
<body>
  <h1>🧠 تحلیل احساسات</h1>
  
  <textarea id="text" placeholder="متن خودتون رو اینجا بنویسید..."></textarea>
  <br><br>
  <button onclick="analyze()">تحلیل کن</button>
  
  <div id="result"></div>

  <script>
    async function analyze() {
      const text = document.getElementById('text').value;
      
      if (!text) {
        alert('لطفاً متنی وارد کنید');
        return;
      }
      
      const url = 'https://really-amin-datasourceforcryptocurrency-2.hf.space/api/sentiment/analyze';
      
      try {
        const response = await fetch(url, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({ text: text })
        });
        
        const data = await response.json();
        
        const result = document.getElementById('result');
        result.innerHTML = `
          <h3>نتیجه تحلیل:</h3>
          <p><strong>احساسات:</strong> ${data.sentiment}</p>
          <p><strong>امتیاز:</strong> ${data.score}</p>
          <p><strong>اطمینان:</strong> ${data.confidence}</p>
        `;
        
      } catch (error) {
        console.error('خطا:', error);
      }
    }
  </script>
</body>
</html>
```

---

#### مثال 3: نمودار زنده با Chart.js
```html
<!DOCTYPE html>
<html>
<head>
  <title>نمودار زنده</title>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
  <h1>📈 نمودار قیمت بیت کوین</h1>
  <canvas id="chart" width="800" height="400"></canvas>

  <script>
    const ctx = document.getElementById('chart').getContext('2d');
    
    const chart = new Chart(ctx, {
      type: 'line',
      data: {
        labels: [],
        datasets: [{
          label: 'قیمت بیت کوین (USD)',
          data: [],
          borderColor: 'rgb(75, 192, 192)',
          tension: 0.1
        }]
      },
      options: {
        responsive: true,
        scales: {
          y: {
            beginAtZero: false
          }
        }
      }
    });
    
    async function updateChart() {
      const url = 'https://really-amin-datasourceforcryptocurrency-2.hf.space/api/market/history?symbol=BTCUSDT&interval=1h&limit=24';
      
      try {
        const response = await fetch(url);
        const data = await response.json();
        
        chart.data.labels = data.data.map(candle => {
          const date = new Date(candle.timestamp);
          return date.toLocaleTimeString('fa-IR', { hour: '2-digit', minute: '2-digit' });
        });
        
        chart.data.datasets[0].data = data.data.map(candle => candle.close);
        
        chart.update();
        
      } catch (error) {
        console.error('خطا:', error);
      }
    }
    
    // بارگذاری اولیه
    updateChart();
    
    // به‌روزرسانی هر 5 دقیقه
    setInterval(updateChart, 5 * 60 * 1000);
  </script>
</body>
</html>
```

---

### روش 3: Python (برای ربات یا تحلیل)

#### مثال 1: گرفتن قیمت‌ها
```python
import requests

def get_prices():
    url = "https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/market?limit=10"
    
    response = requests.get(url)
    data = response.json()
    
    print(f"✅ دریافت {data['count']} ارز")
    
    for coin in data['items']:
        name = coin.get('name', 'Unknown')
        price = coin.get('current_price', 0)
        change = coin.get('price_change_percentage_24h', 0)
        
        print(f"{name}: ${price:,.2f} ({change:+.2f}%)")

# اجرا
get_prices()
```

---

#### مثال 2: تحلیل احساسات
```python
import requests

def analyze_sentiment(text):
    url = "https://really-amin-datasourceforcryptocurrency-2.hf.space/api/sentiment/analyze"
    
    payload = {"text": text}
    
    response = requests.post(url, json=payload)
    data = response.json()
    
    print(f"متن: {text}")
    print(f"احساسات: {data['sentiment']}")
    print(f"امتیاز: {data['score']}")
    print(f"اطمینان: {data['confidence']}")

# تست
texts = [
    "Bitcoin is going to the moon! 🚀",
    "Market crash! Sell everything!",
    "Ethereum upgrade is looking promising"
]

for text in texts:
    analyze_sentiment(text)
    print("-" * 50)
```

---

#### مثال 3: ربات اعلان قیمت
```python
import requests
import time

def price_alert_bot():
    # تنظیمات
    symbol = "bitcoin"
    alert_price = 45000  # قیمت هشدار
    check_interval = 60  # هر 60 ثانیه
    
    print(f"🤖 ربات شروع شد!")
    print(f"📊 نظارت بر {symbol}")
    print(f"🔔 هشدار در قیمت ${alert_price:,}")
    
    while True:
        url = f"https://really-amin-datasourceforcryptocurrency-2.hf.space/api/data/prices/{symbol}"
        
        try:
            response = requests.get(url)
            data = response.json()
            
            current_price = data['current_price']
            
            print(f"قیمت فعلی: ${current_price:,.2f}")
            
            # بررسی هشدار
            if current_price >= alert_price:
                print("🔔" * 10)
                print(f"⚠️ هشدار! قیمت به ${current_price:,.2f} رسید!")
                print("🔔" * 10)
                break
            
        except Exception as e:
            print(f"❌ خطا: {e}")
        
        time.sleep(check_interval)

# اجرا
price_alert_bot()
```

---

#### مثال 4: ذخیره قیمت‌ها در فایل CSV
```python
import requests
import csv
from datetime import datetime

def save_prices_to_csv():
    url = "https://really-amin-datasourceforcryptocurrency-2.hf.space/api/smart/market?limit=50"
    
    response = requests.get(url)
    data = response.json()
    
    # نام فایل با تاریخ و زمان
    filename = f"prices_{datetime.now().strftime('%Y%m%d_%H%M%S')}.csv"
    
    with open(filename, 'w', newline='', encoding='utf-8') as file:
        writer = csv.writer(file)
        
        # هدر
        writer.writerow(['Name', 'Symbol', 'Price', 'Market Cap', '24h Change'])
        
        # داده‌ها
        for coin in data['items']:
            writer.writerow([
                coin.get('name', ''),
                coin.get('symbol', ''),
                coin.get('current_price', 0),
                coin.get('market_cap', 0),
                coin.get('price_change_percentage_24h', 0)
            ])
    
    print(f"✅ ذخیره شد: {filename}")

# اجرا
save_prices_to_csv()
```

---

### روش 4: Node.js (برای Backend)

#### مثال: API Proxy
```javascript
const express = require('express');
const axios = require('axios');

const app = express();
const BASE_URL = 'https://really-amin-datasourceforcryptocurrency-2.hf.space';

// قیمت‌ها
app.get('/prices', async (req, res) => {
  try {
    const limit = req.query.limit || 50;
    const response = await axios.get(`${BASE_URL}/api/smart/market?limit=${limit}`);
    res.json(response.data);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

// احساسات
app.post('/sentiment', async (req, res) => {
  try {
    const response = await axios.post(`${BASE_URL}/api/sentiment/analyze`, {
      text: req.body.text
    });
    res.json(response.data);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

app.listen(3000, () => {
  console.log('✅ سرور در پورت 3000 شروع شد');
});
```

---

### روش 5: WebSocket (Real-time)

#### JavaScript
```javascript
// اتصال
const ws = new WebSocket('wss://really-amin-datasourceforcryptocurrency-2.hf.space/ws');

// وصل شد
ws.onopen = () => {
  console.log('✅ وصل شد');
  
  // عضویت در کانال قیمت‌ها
  ws.send(JSON.stringify({
    action: 'subscribe',
    channel: 'prices',
    symbols: ['BTCUSDT', 'ETHUSDT', 'BNBUSDT']
  }));
};

// دریافت پیام
ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  
  if (data.channel === 'prices') {
    console.log(`📊 ${data.symbol}: $${data.price}`);
    
    // به‌روزرسانی UI
    document.getElementById(`price-${data.symbol}`).textContent = `$${data.price}`;
  }
};

// خطا
ws.onerror = (error) => {
  console.error('❌ خطا:', error);
};

// قطع
ws.onclose = () => {
  console.log('⚠️ اتصال قطع شد');
  // تلاش مجدد بعد از 5 ثانیه
  setTimeout(() => {
    window.location.reload();
  }, 5000);
};
```

---

## 💡 نکات مهم

### 1. همیشه از آدرس Space استفاده کنید
```
✅ https://really-amin-datasourceforcryptocurrency-2.hf.space
❌ http://localhost:7860
```

### 2. Smart Endpoints برای Reliability
```
✅ /api/smart/market  → خودکار Fallback
❌ /api/market        → فقط یک منبع
```

### 3. Error Handling
```javascript
try {
  const response = await fetch(url);
  if (!response.ok) throw new Error('خطا');
  const data = await response.json();
} catch (error) {
  console.error('مشکل:', error);
  // Fallback یا پیام خطا به کاربر
}
```

### 4. Cache کردن
```javascript
// Cache برای 30 ثانیه
const CACHE_TIME = 30 * 1000;
let cache = { data: null, timestamp: 0 };

async function getDataWithCache() {
  const now = Date.now();
  
  if (cache.data && (now - cache.timestamp) < CACHE_TIME) {
    return cache.data; // برگشت از Cache
  }
  
  // گرفتن جدید
  const response = await fetch(url);
  cache.data = await response.json();
  cache.timestamp = now;
  
  return cache.data;
}
```

---

## 🎯 چه کاری می‌تونید بکنید؟

### 1. داشبورد قیمت
- نمایش قیمت 50 ارز برتر
- به‌روزرسانی خودکار هر 30 ثانیه
- نمودارهای تعاملی

### 2. ربات اعلان
- هشدار قیمت
- هشدار تغییرات بزرگ
- اعلان اخبار مهم

### 3. تحلیلگر احساسات
- تحلیل اخبار
- تحلیل توییت‌ها
- تحلیل نظرات

### 4. Portfolio Tracker
- ردیابی سبد
- محاسبه سود/ضرر
- نمایش نمودار

### 5. Trading Bot
- سیگنال خرید/فروش
- تحلیل تکنیکال
- Risk Management

---

## 📚 مستندات بیشتر

### Swagger UI (مستندات تعاملی)
```
https://really-amin-datasourceforcryptocurrency-2.hf.space/docs
```
اینجا می‌تونید تمام endpoint ها رو ببینید و تست کنید!

### مستندات فارسی
- `راهنمای_استفاده_API_فارسی.md` - راهنمای کامل فارسی
- `API_USAGE_GUIDE_COMPLETE.md` - راهنمای کامل انگلیسی
- `API_ENDPOINTS_QUICK_REFERENCE.md` - مرجع سریع endpoint ها
- `🚀_سرویس‌های_موجود_فارسی.md` - لیست تمام سرویس‌ها

---

## 🚀 شروع کنید!

برای شروع، فقط کافیه یکی از مثال‌های بالا رو کپی کنید و اجرا کنید!

**همه چیز آماده است. موفق باشید! 🎉**
