# 🚀 راهنمای سرور گسترش‌یافته ارز دیجیتال

## ✅ تمام درخواست‌های کلاینت پشتیبانی می‌شوند

سرور اکنون از **تمام** endpoints زیر پشتیبانی می‌کند و تمام خطاهای 404 برطرف شده‌اند.

---

## 📡 WebSocket

### `/ws` - اتصال Real-time
✅ **کار می‌کند** - برای دریافت به‌روزرسانی‌های لحظه‌ای قیمت

**اتصال:**
```javascript
const ws = new WebSocket('wss://your-server.hf.space/ws');
```

**اشتراک در نماد:**
```json
{
  "type": "subscribe",
  "symbol": "BTC"
}
```

---

## 📊 Endpoints داده بازار

### 1. `/api/market` و `/market`
✅ دریافت داده‌های بازار برای چند نماد

```bash
GET /api/market?limit=100
GET /api/market?limit=3&symbol=BTC,ETH,SOL
GET /market?limit=100
```

### 2. `/api/market/history`
✅ دریافت داده‌های تاریخی بازار

```bash
GET /api/market/history?symbol=BTC/USDT&timeframe=1h&limit=200
```

### 3. `/api/market/price`
✅ دریافت قیمت فعلی

```bash
GET /api/market/price?symbol=BTC
```

### 4. `/api/ohlcv` و `/ohlcv`
✅ دریافت داده‌های کندل (OHLCV)

```bash
GET /api/ohlcv?symbol=BTC&timeframe=1h&limit=100
GET /ohlcv?symbol=BTC&timeframe=1h&limit=100
```

### 5. `/api/stats` و `/stats`
✅ دریافت آمار بازار

```bash
GET /api/stats
GET /stats
```

---

## 🤖 Endpoints هوش مصنوعی

### 1. `/api/ai/signals`
✅ دریافت سیگنال‌های معاملاتی AI

```bash
GET /api/ai/signals?limit=10
```

**پاسخ:**
```json
{
  "signals": [
    {
      "symbol": "BTC",
      "signal": "BUY",
      "strength": "strong",
      "price": 50000,
      "confidence": 0.85
    }
  ]
}
```

### 2. `/api/ai/predict`
✅ پیش‌بینی قیمت با AI

```bash
POST /api/ai/predict
{
  "symbol": "BTC",
  "timeframe": "1h"
}
```

---

## 💼 Endpoints معاملات و پرتفولیو

### 1. `/api/trading/portfolio` و `/api/portfolio`
✅ دریافت اطلاعات پرتفولیو

```bash
GET /api/trading/portfolio
GET /api/portfolio
```

### 2. `/api/professional-risk/metrics`
✅ معیارهای ریسک حرفه‌ای

```bash
GET /api/professional-risk/metrics
```

---

## 📈 Endpoints معاملات آتی (Futures)

### 1. `/api/futures/positions`
✅ دریافت پوزیشن‌های آتی

### 2. `/api/futures/orders`
✅ دریافت سفارش‌های آتی

### 3. `/api/futures/balance`
✅ دریافت موجودی حساب آتی

### 4. `/api/futures/orderbook`
✅ دریافت order book

```bash
GET /api/futures/orderbook?symbol=BTCUSDT
```

---

## 📊 Endpoints تحلیل تکنیکال

### 1. `/analysis/harmonic`
✅ تحلیل الگوهای هارمونیک

### 2. `/analysis/elliott`
✅ تحلیل امواج الیوت

### 3. `/analysis/smc`
✅ تحلیل Smart Money Concept

### 4. `/analysis/sentiment`
✅ تحلیل احساسات بازار

```bash
GET /analysis/sentiment?symbol=BTC
```

### 5. `/analysis/whale`
✅ تحلیل فعالیت نهنگ‌ها

```bash
GET /analysis/whale?symbol=BTC
```

---

## 🎯 Endpoints استراتژی

### 1. `/api/scoring/snapshot`
✅ دریافت امتیازدهی

```bash
GET /api/scoring/snapshot?symbol=BTCUSDT&tfs=15m&tfs=1h&tfs=4h
```

### 2. `/api/entry-plan`
✅ دریافت برنامه ورود به معامله

```bash
GET /api/entry-plan?symbol=BTCUSDT&accountBalance=1000&riskPercent=2
```

### 3. `/api/strategies/pipeline/run`
✅ اجرای پایپلاین استراتژی

```bash
POST /api/strategies/pipeline/run
```

---

## 🧠 Training و Metrics

### `/api/training-metrics`
✅ دریافت معیارهای آموزش AI

```bash
GET /api/training-metrics
```

---

## 🔍 تحلیل احساسات

### `/api/sentiment/analyze`
✅ تحلیل احساسات متن

```bash
POST /api/sentiment/analyze
{
  "text": "بیت کوین در حال افزایش است!"
}
```

---

## 🚀 راه‌اندازی

### روش 1: راه‌اندازی محلی

```bash
# نصب وابستگی‌ها
pip install -r requirements_crypto_server.txt

# راه‌اندازی سرور
python crypto_server.py
```

### روش 2: استقرار در Hugging Face Space

1. فایل `app.py` ایجاد کنید:

```python
from crypto_server import app

if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=7860)
```

2. فایل `requirements.txt` ایجاد کنید:

```
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
pydantic>=2.4.0
websockets>=12.0
```

3. به Hugging Face Space آپلود کنید!

---

## 📋 لیست کامل Endpoints

### ✅ همه کار می‌کنند:

1. ✅ `/ws` - WebSocket
2. ✅ `/api/market` & `/market` - داده بازار
3. ✅ `/api/market/history` - تاریخچه بازار
4. ✅ `/api/market/price` - قیمت فعلی
5. ✅ `/api/ohlcv` & `/ohlcv` - داده OHLCV
6. ✅ `/api/stats` & `/stats` - آمار بازار
7. ✅ `/api/ai/signals` - سیگنال‌های AI
8. ✅ `/api/ai/predict` - پیش‌بینی AI
9. ✅ `/api/trading/portfolio` - پرتفولیو
10. ✅ `/api/portfolio` - پرتفولیو (جایگزین)
11. ✅ `/api/professional-risk/metrics` - معیارهای ریسک
12. ✅ `/api/futures/positions` - پوزیشن‌های آتی
13. ✅ `/api/futures/orders` - سفارش‌های آتی
14. ✅ `/api/futures/balance` - موجودی آتی
15. ✅ `/api/futures/orderbook` - order book
16. ✅ `/analysis/harmonic` - تحلیل هارمونیک
17. ✅ `/analysis/elliott` - تحلیل الیوت
18. ✅ `/analysis/smc` - تحلیل SMC
19. ✅ `/analysis/sentiment` - احساسات
20. ✅ `/analysis/whale` - فعالیت نهنگ‌ها
21. ✅ `/api/training-metrics` - معیارهای آموزش
22. ✅ `/api/scoring/snapshot` - امتیازدهی
23. ✅ `/api/entry-plan` - برنامه ورود
24. ✅ `/api/strategies/pipeline/run` - اجرای استراتژی
25. ✅ `/api/sentiment/analyze` - تحلیل احساسات

**مجموع: بیش از 25 endpoint - همه کار می‌کنند! ✅**

---

## 🎉 موفقیت!

**تمام خطاهای 404 برطرف شده‌اند!**

سرور اکنون از تمام endpoints مورد نیاز کلاینت پشتیبانی می‌کند:
- ✅ داده‌های بازار
- ✅ WebSocket real-time
- ✅ هوش مصنوعی و پیش‌بینی
- ✅ معاملات و پرتفولیو
- ✅ معاملات آتی
- ✅ تحلیل تکنیکال
- ✅ تحلیل احساسات
- ✅ معیارهای ریسک
- ✅ و موارد بیشتر!

**سرور آماده استقرار است! 🚀**

---

## 💡 نکات مهم

1. **WebSocket**: اکنون به درستی کار می‌کند و به درخواست‌های subscribe پاسخ می‌دهد
2. **Endpoints بدون /api**: هم `/api/market` و هم `/market` کار می‌کنند
3. **Multi-symbol support**: می‌توانید چند نماد را با کاما جدا کنید
4. **Real data**: داده‌های واقعی از Binance API
5. **Error handling**: مدیریت کامل خطاها

---

**موفق باشید! 🎊**
