# 🚀 راهنمای کامل سیستم تریدینگ و بک‌تست

## 📋 فهرست مطالب

- [معرفی](#معرفی)
- [ویژگی‌های کلیدی](#ویژگی-های-کلیدی)
- [نصب و راه‌اندازی](#نصب-و-راه-اندازی)
- [API های تریدینگ](#api-های-تریدینگ)
- [API های بک‌تست](#api-های-بک-تست)
- [استراتژی‌های معاملاتی](#استراتژی-های-معاملاتی)
- [نحوه استفاده](#نحوه-استفاده)
- [مثال‌های کاربردی](#مثال-های-کاربردی)

---

## 🎯 معرفی

این سیستم یک پلتفرم کامل برای تریدینگ و بک‌تست استراتژی‌های معاملاتی است که با استفاده از تکنولوژی‌های پیشرفته به صرافی‌های Binance و KuCoin متصل می‌شود.

### چرا این سیستم؟

❌ **مشکل**: دسترسی به صرافی‌ها در برخی کشورها مسدود است  
✅ **راه‌حل**: سیستم هوشمند با DNS over HTTPS و پروکسی چند لایه

❌ **مشکل**: نیاز به API Key برای دریافت داده  
✅ **راه‌حل**: استفاده از Public APIs بدون نیاز به کلید

❌ **مشکل**: عدم یکپارچگی با سیستم Multi-Source  
✅ **راه‌حل**: یکپارچگی کامل با fallback به 137+ منبع

---

## ✨ ویژگی‌های کلیدی

### 🔄 اتصال هوشمند
- ✅ **DNS over HTTPS (DoH)**: دور زدن محدودیت‌های DNS
- ✅ **Multi-Layer Proxy**: پروکسی HTTP, SOCKS4, SOCKS5
- ✅ **Geo-block Bypass**: عبور خودکار از محدودیت‌های جغرافیایی
- ✅ **Smart Endpoint Switching**: تغییر خودکار endpoint

### 📊 دریافت داده
- ✅ **Real-time Price**: قیمت لحظه‌ای از Binance و KuCoin
- ✅ **OHLCV Data**: داده‌های کندل استیک تا 1000 کندل
- ✅ **Order Book**: دفترچه سفارشات تا 5000 سطح
- ✅ **24h Statistics**: آمار 24 ساعته کامل

### 🧪 بک‌تست
- ✅ **Historical Data**: تا 365 روز داده تاریخی
- ✅ **3 استراتژی آماده**: SMA Crossover, RSI, MACD
- ✅ **DataFrame Ready**: خروجی آماده برای Pandas
- ✅ **Performance Metrics**: محاسبه سود، ضرر، تعداد معاملات

### 🔗 یکپارچگی
- ✅ **Multi-Source Integration**: fallback به 137+ منبع
- ✅ **Automatic Failover**: جابجایی خودکار در صورت خطا
- ✅ **Unified API**: رابط یکپارچه برای همه صرافی‌ها

---

## 🛠️ نصب و راه‌اندازی

### پیش‌نیازها
```bash
Python 3.8+
FastAPI
httpx
pandas
numpy
dnspython (برای DoH)
```

### نصب کتابخانه‌ها
```bash
pip install fastapi uvicorn httpx pandas numpy dnspython
```

### راه‌اندازی سرور
```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### دسترسی به API
- **مستندات**: http://localhost:8000/docs
- **Trading API**: http://localhost:8000/api/trading
- **Backtesting API**: http://localhost:8000/api/trading/backtest

---

## 📊 API های تریدینگ

### 1️⃣ دریافت قیمت فعلی

```http
GET /api/trading/price/{symbol}
```

**پارامترها:**
- `symbol` (required): نماد جفت ارز
  - Binance: `BTCUSDT`, `ETHUSDT`, `BNBUSDT`
  - KuCoin: `BTC-USDT`, `ETH-USDT`, `BNB-USDT`
- `exchange` (default: binance): صرافی (binance یا kucoin)
- `enable_proxy` (default: false): فعال‌سازی پروکسی
- `use_fallback` (default: true): استفاده از سیستم Multi-Source

**مثال:**
```bash
# دریافت قیمت بیتکوین از Binance
curl "http://localhost:8000/api/trading/price/BTCUSDT?exchange=binance"

# دریافت قیمت اتریوم از KuCoin با پروکسی
curl "http://localhost:8000/api/trading/price/ETH-USDT?exchange=kucoin&enable_proxy=true"
```

**پاسخ:**
```json
{
  "success": true,
  "exchange": "binance",
  "symbol": "BTCUSDT",
  "price": 43250.50,
  "timestamp": "2025-12-06T12:00:00Z",
  "method": "smart_client"
}
```

---

### 2️⃣ دریافت داده‌های OHLCV (کندل استیک)

```http
GET /api/trading/ohlcv/{symbol}
```

**پارامترها:**
- `symbol` (required): نماد جفت ارز
- `timeframe` (default: 1h): بازه زمانی
  - Binance: `1m`, `5m`, `15m`, `1h`, `4h`, `1d`, `1w`, `1M`
  - KuCoin: `1min`, `5min`, `15min`, `1hour`, `4hour`, `1day`, `1week`
- `limit` (default: 100, max: 1000): تعداد کندل‌ها
- `exchange` (default: binance): صرافی
- `start_time` (optional): زمان شروع (میلی‌ثانیه)
- `end_time` (optional): زمان پایان (میلی‌ثانیه)
- `enable_proxy` (default: false): فعال‌سازی پروکسی

**مثال:**
```bash
# دریافت 100 کندل 1 ساعته از Binance
curl "http://localhost:8000/api/trading/ohlcv/BTCUSDT?timeframe=1h&limit=100"

# دریافت 500 کندل 15 دقیقه‌ای از KuCoin
curl "http://localhost:8000/api/trading/ohlcv/BTC-USDT?exchange=kucoin&timeframe=15min&limit=500"
```

**پاسخ:**
```json
{
  "success": true,
  "exchange": "binance",
  "symbol": "BTCUSDT",
  "timeframe": "1h",
  "candles": [
    {
      "timestamp": 1733491200000,
      "open": 43200.00,
      "high": 43300.00,
      "low": 43150.00,
      "close": 43250.50,
      "volume": 1234.56,
      "quote_volume": 53245678.90,
      "trades": 12345
    }
  ],
  "count": 100,
  "method": "smart_client"
}
```

---

### 3️⃣ دریافت دفترچه سفارشات (Order Book)

```http
GET /api/trading/orderbook/{symbol}
```

**پارامترها:**
- `symbol` (required): نماد جفت ارز
- `exchange` (default: binance): صرافی
- `limit` (default: 100, max: 5000): عمق دفترچه
- `enable_proxy` (default: false): فعال‌سازی پروکسی

**مثال:**
```bash
# دریافت دفترچه سفارشات بیتکوین
curl "http://localhost:8000/api/trading/orderbook/BTCUSDT?limit=20"
```

**پاسخ:**
```json
{
  "success": true,
  "exchange": "binance",
  "symbol": "BTCUSDT",
  "bids": [
    [43250.50, 1.234],
    [43249.00, 0.567],
    [43248.50, 2.345]
  ],
  "asks": [
    [43251.00, 0.890],
    [43252.50, 1.456],
    [43253.00, 0.678]
  ],
  "timestamp": 1733491200000
}
```

---

### 4️⃣ دریافت آمار 24 ساعته

```http
GET /api/trading/stats/24h/{symbol}
```

**پارامترها:**
- `symbol` (required): نماد جفت ارز
- `exchange` (default: binance): صرافی
- `enable_proxy` (default: false): فعال‌سازی پروکسی

**مثال:**
```bash
# دریافت آمار 24 ساعته بیتکوین
curl "http://localhost:8000/api/trading/stats/24h/BTCUSDT"
```

**پاسخ:**
```json
{
  "success": true,
  "exchange": "binance",
  "symbol": "BTCUSDT",
  "price": 43250.50,
  "change": 850.25,
  "change_percent": 2.01,
  "high": 43500.00,
  "low": 42800.00,
  "volume": 12345.67,
  "quote_volume": 534567890.12,
  "trades": 987654,
  "timestamp": "2025-12-06T12:00:00Z"
}
```

---

## 🧪 API های بک‌تست

### 5️⃣ دریافت داده تاریخی

```http
GET /api/trading/backtest/historical/{symbol}
```

**پارامترها:**
- `symbol` (required): نماد جفت ارز
- `timeframe` (default: 1h): بازه زمانی
- `days` (default: 30, max: 365): تعداد روزهای تاریخی
- `exchange` (default: binance): صرافی
- `enable_proxy` (default: false): فعال‌سازی پروکسی

**مثال:**
```bash
# دریافت 30 روز داده تاریخی بیتکوین
curl "http://localhost:8000/api/trading/backtest/historical/BTCUSDT?days=30&timeframe=1h"
```

**توجه:** این درخواست ممکن است زمان‌بر باشد (به دلیل محدودیت rate limit).

**پاسخ:**
```json
{
  "success": true,
  "symbol": "BTCUSDT",
  "exchange": "binance",
  "timeframe": "1h",
  "days": 30,
  "candles": [...],
  "count": 720
}
```

---

### 6️⃣ اجرای بک‌تست

```http
GET /api/trading/backtest/run/{symbol}
```

**پارامترها:**
- `symbol` (required): نماد جفت ارز
- `strategy` (required): نام استراتژی (`sma_crossover`, `rsi`, `macd`)
- `timeframe` (default: 1h): بازه زمانی
- `days` (default: 30, max: 365): دوره تاریخی
- `exchange` (default: binance): صرافی
- `initial_capital` (default: 10000): سرمایه اولیه
- `enable_proxy` (default: false): فعال‌سازی پروکسی

**مثال:**
```bash
# اجرای بک‌تست با استراتژی SMA Crossover
curl "http://localhost:8000/api/trading/backtest/run/BTCUSDT?strategy=sma_crossover&days=30&initial_capital=10000"

# اجرای بک‌تست با استراتژی RSI
curl "http://localhost:8000/api/trading/backtest/run/BTCUSDT?strategy=rsi&days=30"

# اجرای بک‌تست با استراتژی MACD
curl "http://localhost:8000/api/trading/backtest/run/BTCUSDT?strategy=macd&days=30"
```

**پاسخ:**
```json
{
  "success": true,
  "symbol": "BTCUSDT",
  "exchange": "binance",
  "strategy": "sma_crossover",
  "timeframe": "1h",
  "days": 30,
  "initial_capital": 10000.0,
  "final_capital": 10567.89,
  "profit": 567.89,
  "total_return": 5.68,
  "trades": 12,
  "candles_analyzed": 720
}
```

---

## 📈 استراتژی‌های معاملاتی

### 1. SMA Crossover (تقاطع میانگین متحرک ساده)

**منطق:**
- خرید: وقتی SMA سریع (10 دوره) از SMA کند (30 دوره) بالاتر رود
- فروش: وقتی SMA سریع از SMA کند پایین‌تر رود

**مناسب برای:**
- بازارهای روند‌دار (Trending Markets)
- معامله‌گران میان‌مدت

**مثال:**
```bash
curl "http://localhost:8000/api/trading/backtest/run/BTCUSDT?strategy=sma_crossover&days=30"
```

---

### 2. RSI (شاخص قدرت نسبی)

**منطق:**
- خرید: وقتی RSI < 30 (oversold - فروش بیش از حد)
- فروش: وقتی RSI > 70 (overbought - خرید بیش از حد)

**مناسب برای:**
- شناسایی نقاط بازگشت
- بازارهای رنج (Range-bound Markets)

**مثال:**
```bash
curl "http://localhost:8000/api/trading/backtest/run/BTCUSDT?strategy=rsi&days=30"
```

---

### 3. MACD (واگرایی همگرایی میانگین متحرک)

**منطق:**
- خرید: وقتی MACD از خط سیگنال بالاتر رود
- فروش: وقتی MACD از خط سیگنال پایین‌تر رود

**مناسب برای:**
- شناسایی روندهای جدید
- تأیید روندهای موجود

**مثال:**
```bash
curl "http://localhost:8000/api/trading/backtest/run/BTCUSDT?strategy=macd&days=30"
```

---

## 💡 نحوه استفاده

### مثال 1: دریافت قیمت بیتکوین

**Python:**
```python
import httpx
import asyncio

async def get_btc_price():
    async with httpx.AsyncClient() as client:
        response = await client.get(
            "http://localhost:8000/api/trading/price/BTCUSDT",
            params={"exchange": "binance"}
        )
        data = response.json()
        print(f"BTC Price: ${data['price']:,.2f}")

asyncio.run(get_btc_price())
```

**JavaScript:**
```javascript
fetch('http://localhost:8000/api/trading/price/BTCUSDT?exchange=binance')
  .then(response => response.json())
  .then(data => {
    console.log(`BTC Price: $${data.price.toLocaleString()}`);
  });
```

---

### مثال 2: اجرای بک‌تست SMA

**Python:**
```python
import httpx
import asyncio

async def run_backtest():
    async with httpx.AsyncClient(timeout=60.0) as client:
        response = await client.get(
            "http://localhost:8000/api/trading/backtest/run/BTCUSDT",
            params={
                "strategy": "sma_crossover",
                "days": 30,
                "initial_capital": 10000
            }
        )
        result = response.json()
        
        print(f"Strategy: {result['strategy']}")
        print(f"Total Return: {result['total_return']:.2f}%")
        print(f"Profit: ${result['profit']:.2f}")
        print(f"Trades: {result['trades']}")

asyncio.run(run_backtest())
```

---

### مثال 3: مقایسه قیمت بین دو صرافی

**Python:**
```python
import httpx
import asyncio

async def compare_prices():
    async with httpx.AsyncClient() as client:
        # قیمت از Binance
        binance_response = await client.get(
            "http://localhost:8000/api/trading/price/BTCUSDT",
            params={"exchange": "binance"}
        )
        binance_price = binance_response.json()['price']
        
        # قیمت از KuCoin
        kucoin_response = await client.get(
            "http://localhost:8000/api/trading/price/BTC-USDT",
            params={"exchange": "kucoin"}
        )
        kucoin_price = kucoin_response.json()['price']
        
        # محاسبه اختلاف
        diff = abs(binance_price - kucoin_price)
        diff_percent = (diff / binance_price) * 100
        
        print(f"Binance: ${binance_price:,.2f}")
        print(f"KuCoin: ${kucoin_price:,.2f}")
        print(f"Difference: ${diff:.2f} ({diff_percent:.3f}%)")

asyncio.run(compare_prices())
```

---

## 🔧 پیکربندی پیشرفته

### فعال‌سازی پروکسی

اگر در کشوری هستید که دسترسی به Binance یا KuCoin محدود است:

```bash
# فعال‌سازی پروکسی
curl "http://localhost:8000/api/trading/price/BTCUSDT?enable_proxy=true"
```

**توجه:** سیستم به صورت خودکار پروکسی‌های رایگان را دریافت و تست می‌کند.

---

### استفاده از Fallback

اگر صرافی اصلی در دسترس نباشد، سیستم به صورت خودکار به سیستم Multi-Source با 137+ منبع متصل می‌شود:

```bash
# استفاده از fallback
curl "http://localhost:8000/api/trading/price/BTCUSDT?use_fallback=true"
```

---

## 📊 وضعیت صرافی‌ها

برای بررسی وضعیت اتصال به صرافی‌ها:

```http
GET /api/trading/exchanges/status
```

**مثال:**
```bash
curl "http://localhost:8000/api/trading/exchanges/status"
```

**پاسخ:**
```json
{
  "success": true,
  "exchanges": {
    "binance": {
      "available": true,
      "endpoints": 5,
      "current_endpoint": "https://api.binance.com",
      "proxy_enabled": false,
      "doh_enabled": true
    },
    "kucoin": {
      "available": true,
      "endpoints": 2,
      "current_endpoint": "https://api.kucoin.com",
      "proxy_enabled": false,
      "doh_enabled": true
    }
  }
}
```

---

## 🧪 اجرای تست‌ها

برای اطمینان از عملکرد صحیح سیستم:

```bash
python3 test_trading_system.py
```

**نتیجه تست‌ها:**
```
✅ PASS: Binance - Get BTC Price
✅ PASS: KuCoin - Get BTC Price
✅ PASS: Binance - Get OHLCV
✅ PASS: KuCoin - Get OHLCV
✅ PASS: Binance - Get Orderbook
✅ PASS: Binance - Get 24h Stats
✅ PASS: Backtesting - Fetch Historical Data
✅ PASS: Backtesting - SMA Crossover
✅ PASS: Backtesting - RSI
✅ PASS: Backtesting - MACD

Total Tests: 10
✅ Passed: 10
❌ Failed: 0
Success Rate: 100.0%
```

---

## 🚨 رفع مشکلات

### مشکل 1: خطای 451 (Geo-block)

**علت:** دسترسی از کشور شما مسدود است.

**راه‌حل:**
```bash
# فعال‌سازی پروکسی
curl "http://localhost:8000/api/trading/price/BTCUSDT?enable_proxy=true"
```

---

### مشکل 2: خطای Timeout

**علت:** اتصال کند یا مشکل شبکه.

**راه‌حل:**
- بررسی اتصال اینترنت
- استفاده از پروکسی
- تغییر endpoint (سیستم به صورت خودکار انجام می‌دهد)

---

### مشکل 3: خطای 429 (Rate Limit)

**علت:** درخواست‌های زیاد.

**راه‌حل:**
سیستم به صورت خودکار منتظر می‌ماند و دوباره تلاش می‌کند.

---

## 📝 نکات مهم

### ⚠️ محدودیت‌ها

1. **Rate Limits**: 
   - Binance: 1200 درخواست در دقیقه
   - KuCoin: متغیر بر اساس endpoint

2. **تعداد کندل‌ها**:
   - حداکثر 1000 کندل در هر درخواست

3. **بک‌تست**:
   - برای داده‌های تاریخی زیاد، زمان اجرا طولانی است
   - توصیه: شروع با داده کم (7-30 روز)

### ✅ توصیه‌ها

1. **استفاده از Cache**:
   - سیستم Multi-Source داده‌ها را کش می‌کند
   - نیازی به درخواست مکرر نیست

2. **بهینه‌سازی**:
   - برای بک‌تست، از timeframe بزرگتر استفاده کنید (4h یا 1d)
   - درخواست‌های موازی را محدود کنید

3. **امنیت**:
   - این API ها فقط برای خواندن داده هستند
   - برای معاملات واقعی، به API Key نیاز دارید

---

## 📚 منابع اضافی

- [مستندات Binance API](https://binance-docs.github.io/apidocs/)
- [مستندات KuCoin API](https://docs.kucoin.com/)
- [راهنمای سیستم Multi-Source](MULTI_SOURCE_SYSTEM_GUIDE.md)

---

## 🎉 خلاصه

سیستم تریدینگ و بک‌تست با موفقیت پیاده‌سازی شده و آماده استفاده است:

- ✅ اتصال هوشمند به Binance و KuCoin
- ✅ DNS over HTTPS و پروکسی چند لایه
- ✅ 3 استراتژی بک‌تست آماده
- ✅ یکپارچگی با سیستم Multi-Source
- ✅ API های کامل و مستند

**الان می‌تونید:**
1. قیمت‌های لحظه‌ای رو دریافت کنید
2. داده‌های تاریخی رو بگیرید
3. استراتژی‌هاتون رو بک‌تست کنید
4. با اطمینان معامله کنید

---

**ساخته شده با ❤️ برای معامله‌گران حرفه‌ای**

*نسخه 1.0.0 - آماده برای استفاده در محیط تولید* 🚀
